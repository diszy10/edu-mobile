import 'package:flutter/material.dart';

class ActivityClass {
  final String timeStamp, content;
  final Color shapeColor, iconColor;

  ActivityClass(
      {this.timeStamp, this.content, this.shapeColor, this.iconColor});
}